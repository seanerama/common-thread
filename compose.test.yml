services:
  db:
    image: ${TEST_POSTGRES_IMAGE:?Set the platform manifest from the pinned index}
    platform: ${DOCKER_DEFAULT_PLATFORM:?Set the test platform}
  app:
    ports:
      - "127.0.0.1:${TEST_HTTP_PORT:-18010}:8000"
